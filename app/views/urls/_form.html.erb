<%= form_for(@url) do |f| %>
  <% if @url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@url.errors.count, "error") %> prohibited this url from being saved:</h2>

      <ul>
      <% @url.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :enable %><br>
    <%= f.number_field :enable %>
  </div>
  <div class="field">
    <%= f.label :need_date %><br>
    <%= f.number_field :need_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
